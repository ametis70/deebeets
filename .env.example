# Copy this file to .env and fill in your value.
# .env is gitignored — never commit it.

# Your Deezer ARL cookie (required).
# Find it in browser DevTools → Application → Cookies → deezer.com → arl
DEEBEETS_ARL=
