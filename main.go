// Command deeznt syncs and downloads your Deezer favorites, tags them for
// Navidrome, and optionally converts them to opus via ffmpeg.
package main

import "deeznt/cmd"

func main() {
	cmd.Execute()
}
